planner:
  name: shooting
  horizon: 8
  num_candidates: 256
  execute_steps: 1
  discount: 0.99
  # A deterministic proposal-mean trajectory anchors every random search.
  include_mean_candidate: true
